import { PageProps } from "@/types";
import DashboardLayout from "@/Layouts/DashboardLayout";

export default function Dashboard({ auth }: PageProps) {
    return (
        <DashboardLayout headTitle="Inicio">
           xaxax
        </DashboardLayout>
    );
}
